#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

// Window dimensions
constexpr int   WINDOW_WIDTH   = 800;
constexpr int   WINDOW_HEIGHT  = 600;

// Paddle and ball sizes
constexpr float PADDLE_WIDTH   = 10.f;
constexpr float PADDLE_HEIGHT  = 100.f;
constexpr float BALL_RADIUS    = 10.f;

// Speeds (pixels per second)
constexpr float PADDLE_SPEED   = 300.f;
constexpr float BALL_SPEED_X   = 250.f;
constexpr float BALL_SPEED_Y   = 250.f;
constexpr int WINNING_SCORE = 5;

#endif // CONSTANTS_HPP